export default function IndexPage() {
  // Keep the global layout (navbar, header, footer) from RootLayout/LayoutClient,
  // but intentionally render an empty main area for now.
  return <main className="px-4 py-6 lg:px-8" />;
}
